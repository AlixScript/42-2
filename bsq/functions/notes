//executable nommer bsq et se trouve dans repertoire principal
//repertoire possede un fichier auteur dans lequel net nos login
//programme prend de 1 a n fichiers en parametres
//si aucun arguments passe programme lis sur l'entree standard


//faire un makefile qui compile le projet


//recuperer le fichier passe en argument
//faire un parsing pour verifier si le fichier donne est bon
	//avoir des lignes de la meme longueur
	//avoir une ligne d'au moins une case
	//a la fin de chaque ligne un retoiur a la ligne
	//caractères présent dans la carte doivent être uniquement ceux présenté à la première ligne
	//cas d'erreur -> map error suivi d'un retour a la ligne

//recuperer les arguments du programme 
	//correspondant aux nombre de ligne du plateu
	//le caractere vide
	//le caractere obstacle
	//le cracatere plein
//faire les cas d'erreur
//


/* 


Générateur de plateaux perl

#!/usr/bin/perl
use warnings;
use strict;
die "program x y density" unless (scalar(@ARGV) == 3);
my ($x, $y, $density) = @ARGV;
print "$y.ox\n";
for (my $i = 0; $i < $y; $i++) {
    for (my $j = 0; $j < $x; $j++) {
if (int(rand($y) * 2) < $density) {
    print "o";
}
else {
print "."; }
}
print "\n"; }


*/


//utilise seulement fonctions exit, open, close, write, read, malloc, free
//verifier la norme
//utiliser uniquement elements vus durant la piscine
